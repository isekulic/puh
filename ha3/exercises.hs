import Data.List
import Data.Char


firstColumn n = [ head c | c <- n ]

-- 2.1
pad :: String -> String -> (String, String)
let addingStuff = take length xs - length ys cycle " " in
pad (x:xs) (y:ys) = (toUpper x:xs, toUpper y:ys ++ addingStuff)
 
--4.1
foobar (a,b) c = case (a,b) of
  

Practical Haskell

Hackage : Haskell Package 

Cabal : Common Architecture for Building Applications and Libraries
www.haskell.org/cabal

cabal install cabal cabal-install -> install latest version

